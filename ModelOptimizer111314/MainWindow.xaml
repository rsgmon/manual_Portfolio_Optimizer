<Window x:Class="ModelOptimizer111314.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525"
        xmlns:vm="clr-namespace:ModelOptimizer111314.ViewModel"
        xmlns:vw="clr-namespace:ModelOptimizer111314.View">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:SymbolWeightsViewModel}">
            <vw:SymbolWeightListView/>
        </DataTemplate>
    </Window.Resources>
    <Grid Margin="4">
        <Border Background="GhostWhite" BorderBrush="LightGray" BorderThickness="1"
                CornerRadius="5">
            <ItemsControl ItemsSource="{Binding ViewModels}" Margin="4"/>
        </Border>
    </Grid>
</Window>
